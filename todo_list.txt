TODO list:

Llama4 Agent mode 해결한다. 작은 모델 내 컴퓨터에 깔아보고 해결해보자

Claude code 공부 더하고 시도해본다.
OpenWebUI Pdf 문제 해결한다.
Packet Generator 다시 만든다. - VLAN MPLS vlan id
Cursor 시도해본다.

python installation


Open Claude Code chatting terminal: Command + esc

-- MAC setup
installed claude code
installed cusor
to install python, I had to install Xcode first (developer tools)
installed emacs
installed vim

brew update
brew install emacs
brew install vim

at my account:
~/.zshrc   파일 셑업
export PATH="/opt/homebrew/bin:$PATH"
source ~/.zprofile

added a few alias:
alias ll='ls -al'
alias em='emacs -nw'


connect github on vscode
connect github for cursor 


export GEMINI_API_KEY="AIzaSyBX_EDzxG2_ionmzwAfIOY9cHhy5xsa0mU"

Gemini CLI on vscode:
{
  "selectedAuthType": "gemini-api-key",
  "theme": "Dracula",
  "preferredEditor": "vscode"
}
